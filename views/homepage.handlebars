<ol> 
    {{#each posts as |post|}}
    <li>
        <div>
            <a href="/post/{{id}}" target="_blank">{{title}}</a>
            <span>Created on {{created_at}}</span>
        </div>
    </li>
    {{/each}}
</ol>